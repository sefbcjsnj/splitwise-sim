# Course Project Paper

This folder contains the final course-paper draft for:

```text
When Does Prefill-Decode Disaggregation Help?
A Parametric Study of Trade-offs in LLM Serving
```

Files:

| Path | Purpose |
| --- | --- |
| `main.tex` | LaTeX source for the report |
| `references.bib` | Paper bibliography |
| `figures/` | Figures copied from `pd_disaggregation_deliverables/figures/` |
| `main.pdf` | Compiled report, generated by LaTeX |

Build:

```powershell
pdflatex main
bibtex main
pdflatex main
pdflatex main
```

The paper uses the compact experiment outputs already committed in:

```text
../pd_disaggregation_deliverables/
```
